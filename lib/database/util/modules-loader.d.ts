import CommandoClient from '../../client';
/**
 * Handlers for the whole database of the client.
 * @param client - The client these handlers are for
 */
export default function modulesLoader(client: CommandoClient): Promise<void>;
